// @flow
import React, { Component } from 'react';
import FirstTimeConfig from '../components/FirstTimeConfig';

export default class FirstTimePage extends Component {
  render() {
    return (
      <FirstTimeConfig />
    );
  }
}
