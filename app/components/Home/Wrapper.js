import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  height: 100%;
  display: flex;

  .settings {
    position: absolute;
    top: 10px;
    left: 10px;
  }
`;

export default Wrapper;
